---
title: Astro Proxy
---

You can use astro to route all analytics traffic through your own server and domain. This will reduce DNS lookups, as well as prevent Adblockers from blocking requests.

By default the tracker sends request to lucid api so change that on that by passing "/api/lucid" to the config host paramter. Then modify your astro.config.ts to include redirect like below.

```ts
export default defineConfig({
    redirects: {
        "/api/lucid": "https://api.lucid.io",
    },
})
```
