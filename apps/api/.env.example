# Similar secret with the next js project
NEXTAUTH_SECRET=

# Clickhouse Database Credentials (optional) by default it uses sqlite. It should be enough for local development but should be changed for production
CLICKHOUSE_HOST=
CLICKHOUSE_PASSWORD=
CLICKHOUSE_DATABASE=
CLICKHOUSE_USER=

# Turso Database Credentials (optional) by default it uses local sqlite database
DATABASE_URL=
DATABSE_AUTH_TOEKN=
